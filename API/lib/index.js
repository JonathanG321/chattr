const GENERAL = 'General Chat';
const JOIN_ROOM = 'join room';
const MESSAGE = 'message';
const LEAVE_ROOM = 'leave room';
const SET_ROOM = 'set room';
const DISABLE_ROOM = 'disable room';
const ENABLE_ROOM = 'enable room';
const socketEvent = {
  GENERAL,
  JOIN_ROOM,
  MESSAGE,
  LEAVE_ROOM,
  SET_ROOM,
  DISABLE_ROOM,
  ENABLE_ROOM,
};

module.exports = socketEvent;
